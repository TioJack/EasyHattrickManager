<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="easyhattrickmanager.repository.PlayerSubSkillDAO">

  <insert id="insert">
    INSERT IGNORE INTO player_subskill (
      id,
      season_week,
      team_id,
      stamina,
      keeper,
      defender,
      playmaker,
      winger,
      passing,
      scorer,
      set_pieces,
      htms,
      htms28
    )
    VALUES (
      #{player.id},
      #{player.seasonWeek},
      #{player.teamId},
      #{player.stamina},
      #{player.keeper},
      #{player.defender},
      #{player.playmaker},
      #{player.winger},
      #{player.passing},
      #{player.scorer},
      #{player.setPieces},
      #{player.htms},
      #{player.htms28}
    );
  </insert>

  <select id="get" resultType="easyhattrickmanager.repository.model.PlayerSubSkill">
    SELECT id,
           season_week,
           team_id,
           stamina,
           keeper,
           defender,
           playmaker,
           winger,
           passing,
           scorer,
           set_pieces,
           htms,
           htms28
    FROM player_subskill
    WHERE team_id = #{teamId}
    ORDER BY season_week;
  </select>

  <select id="getPlayer" resultType="easyhattrickmanager.repository.model.PlayerSubSkill">
    SELECT id,
           season_week,
           team_id,
           stamina,
           keeper,
           defender,
           playmaker,
           winger,
           passing,
           scorer,
           set_pieces,
           htms,
           htms28
    FROM player_subskill
    WHERE team_id = #{teamId}
      AND id = #{playerId}
    ORDER BY season_week;
  </select>

</mapper>
