<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="easyhattrickmanager.repository.LanguageDAO">

  <insert id="insert">
    INSERT INTO language (id, name, friendly_name)
    VALUES (#{language.id}, #{language.name}, #{language.friendlyName}) ON DUPLICATE KEY
    UPDATE
      name = #{language.name},
      friendly_name = #{language.friendlyName};
  </insert>

  <select id="get" resultType="easyhattrickmanager.repository.model.Language">
    SELECT id, name, friendly_name
    FROM language
    WHERE id = #{id};
  </select>

  <select id="getAllLanguages" resultType="easyhattrickmanager.repository.model.Language">
    SELECT id, name, friendly_name
    FROM language
    WHERE friendly_name IS NOT NULL
    ORDER BY name;
  </select>

</mapper>